<?xml version="1.0" encoding="utf-8"?>
<layout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools">
  <data>
    <import type="com.mandriv.keepfit.data.goals.Goal" />
    <variable name="viewModel" type="com.mandriv.keepfit.ui.goals.GoalsViewModel"/>
  </data>
  <androidx.constraintlayout.widget.ConstraintLayout
          android:layout_width="match_parent"
          android:layout_height="match_parent">

    <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            android:id="@+id/active_goal_text_view"
            android:text="@{viewModel.test}"
    />
    <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            android:id="@+id/active_goal_text_view2"
            android:text="@{viewModel.activeGoal.name}"
    />
    <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/goals_recycle_view"
            android:layout_width="0dp"
            android:layout_height="0dp"
            tools:listitem="@layout/goal_item"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toBottomOf="@id/active_goal_text_view"
            app:layout_constraintHorizontal_bias="0.0"
            app:layout_constraintVertical_bias="0.021"/>
    <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fab"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:src="@drawable/ic_add_black_24dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="16dp"
            app:backgroundTint="@color/colorAccent"
            app:tint="@color/whitePrimary"
            android:onClick="@{() -> viewModel.onNewGoalPress(context)}"/>

  </androidx.constraintlayout.widget.ConstraintLayout>
</layout>